bucket = [1, 2, 3, 4, 5]

iterator_bucket = iter(bucket)

value = next(iterator_bucket)
value = next(iterator_bucket)
value = next(iterator_bucket)
value = next(iterator_bucket)
value = next(iterator_bucket)
value = next(iterator_bucket)