class Solution(object):
    def generate(self, numRows):
        numRows = int
        output = List[int]
        if numRows == 0:
            return []
        if numRows == 1:
            return [[1]]



